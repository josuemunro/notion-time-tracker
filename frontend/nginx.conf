server {
  listen 80;
  server_name localhost; # Or your domain

  root /usr/share/nginx/html;
  index index.html index.htm;

  # Serve static files directly
  location ~* \.(?:css|js|jpg|jpeg|gif|png|ico|svg|woff|woff2|ttf|eot)$ {
    expires 1y;
    add_header Cache-Control "public";
  }

  # For all other locations, try files directly and then fall back to index.html for SPA routing
  location / {
    try_files $uri $uri/ /index.html;
  }

  # Optional: Proxy API requests if Nginx is also acting as a reverse proxy for the backend
  # This is an alternative to the frontend making direct calls to the backend container.
  # If using this, the frontend would make requests to /api on its own Nginx server.
  # location /api/ 
  #{
  #  proxy_pass http://backend:3001/api/; # Assumes backend service is named 'backend' and runs on port 3001
  #  proxy_set_header Host $host;
  #  proxy_set_header X-Real-IP $remote_addr;
  #  proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
  #  proxy_set_header X-Forwarded-Proto $scheme;
  #}
}